{% extends 'base.html.twig' %}

{% block title %}Crear rol{% endblock %}

{% block body %}
<h1>Crear rol</h1>
    {{ form_start(form) }}
    <div>
        {{ form_label(form.nombre) }}
        {{ form_widget(form.nombre) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
